version: "3.9"

services:

  django:
    build: ./backend
    container_name: django_backend

    ports:
      - "8000:8000"

    volumes:
      - ./backend:/app

    depends_on:
      - redis

  redis:
    image: redis:7-alpine

    container_name: redis_server

    ports:
      - "6379:6379"